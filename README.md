# NodeJS server for QR Attendance App.

On running the server, a request is accepted which checks for the end point and executes a SQL query. Then it parses the query result in JSON format and sends it as a response.
